# Engine

`template.html` 是最小可运行模板。

- `lecture-engine.css`：主题与布局
- `lecture-engine.js`：课程导航、逐条显示、全屏、讲稿、搜索、批注等核心逻辑
- `demo-data.js`：示例课程数据

复制本目录，修改 `demo-data.js` 即可开始新课程。完成后建议移动到 `courseware/<课程名>/`。
